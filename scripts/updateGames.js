import puppeteer from 'puppeteer'; import fs from 'fs'; import path from 'path';
// ⚡ Hier alle Ligen eintragen 
const ligas = [ { name: '1. Bundesliga Herren', url: 'https://spielplan.rollhockey.de/lm/saison/29/liga/407' }, ];
(async () => { try { const browser = await puppeteer.launch({ headless: true, args: ['--no-sandbox', '--disable-setuid-sandbox', '--disable-dev-shm-usage'], }); await Promise.all( ligas.map(async (liga) => { const page = await browser.newPage(); console.log(Lade Tabelle: ${liga.name}); await page.goto(liga.url, { waitUntil: 'networkidle2' }); await page.waitForSelector('lm-schedule-game-entry-row', { timeout: 60000 }); const table = await page.$$eval('lm-schedule-game-entry-row', (rows) => rows.map((row) => { const text = row.innerText.trim(); const date = text.match(/(\d+)\s*$/); const location = text.match(/(\d+)\s*:\s*(\d+)/); let cleaned = text.replace(/\d+\s*$/, '').replace(/(\d+\s*:\s*\d+)/, '').trim(); const home = cleaned.split(/\s+/); const result = cleaned.split(/\s+/); const away = cleaned.split(/\s+/); return { date,location,home,result,away }; }) ); const fileName = liga.name.replace(/\s+/g, '_') + '.json'; const dataPath = path.join(process.cwd(), 'public/data', fileName); fs.mkdirSync(path.dirname(dataPath), { recursive: true }); fs.writeFileSync(dataPath, JSON.stringify(table, null, 2), 'utf-8'); console.log(Tabelle gespeichert: ${fileName} (${table.length} Teams)); await page.close(); }) ); await browser.close(); console.log('✅ Alle Ligen erfolgreich aktualisiert!'); } catch (err) { console.error('❌ Fehler beim Laden der Tabellen:', err); process.exit(1); } })();
